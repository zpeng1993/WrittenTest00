#include "vectorlearning.h"

int MapLearningMain()
{
	/*
	*		map<string, int> product;
			for(int j=0;j<m;j++)
            {
            		string str;
              		cin >> str;
              		product[str]++;
            }
          	vector<int> nums;
          	for(map<string,int>::iterator iter = product.begin();iter != product.end();++iter)
            {
            		nums.push_back(iter->second);
            }
	*
	*/
	return RET_OK;
}